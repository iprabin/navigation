import { Button, ScrollView, Text } from "react-native";
import { Header, Screen, back } from "app-navigation";

export function EditProfile() {
  return (
    <Screen>
      <ScrollView>
        <Text>edit profile (modal)</Text>
      </ScrollView>
    </Screen>
  );
}
